![Git](https://img.shields.io/badge/Git-F05032?style=for-the-badge&logo=git&logoColor=white)
![GitHub](https://img.shields.io/badge/GitHub-181717?style=for-the-badge&logo=github&logoColor=white)
![Terminal](https://img.shields.io/badge/Terminal-4D4D4D?style=for-the-badge&logo=windows-terminal&logoColor=white)

# 📚 Репозиторий для изучения Git

Репозиторий посвящен изучению системы контроля версий Git. Здесь собраны примеры, конспекты и обучающие материалы из различных источников:

- Открытый курс Яндекса по Git
- Официальная документация Git
- Другие открытые образовательные ресурсы

---

## 🔄 Различие между командами терминала и Git

**Стандартные команды терминала Linux** работают с файловой системой напрямую:
- Управляют файлами и директориями на диске
- Не имеют отношения к системам контроля версий
- Доступны в любой Unix-подобной системе

**Команды Git** работают с репозиторием и системой контроля версий:
- Управляют историей изменений, ветками, коммитами
- Работают через виртуальную файловую систему Git
- Требуют инициализированного репозитория (.git directory)

---

## 🐚 Основные команды Bash/Terminal

### Навигация и просмотр
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `pwd` | `pwd` | Показать путь к текущей папке (Print Working Directory) | Стандартная терминала |
| `cd` | `cd путь/к/папке` | Сменить директорию (Change Directory) | Стандартная терминала |
| `cd ~` | `cd ~` | Перейти в домашнюю директорию пользователя | Стандартная терминала |
| `cd ..` | `cd ..` | Подняться на уровень выше в директориях | Стандартная терминала |
| `ls` | `ls` | Показать содержимое текущей директории (List) | Стандартная терминала |
| `ls -a` | `ls -a` | Показать все файлы, включая скрытые | Стандартная терминала |
| `ls -la` | `ls -la` | Показать подробный список всех файлов | Стандартная терминала |

### Работа с файлами и директориями
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `touch` | `touch имя_файла` | Создать новый пустой файл | Стандартная терминала |
| `mkdir` | `mkdir имя_папки` | Создать новую директорию (Make Directory) | Стандартная терминала |
| `mkdir -p` | `mkdir -p путь/к/папке` | Создать иерархию директорий | Стандартная терминала |
| `cp` | `cp файл папка/` | Скопировать файл (Copy) | Стандартная терминала |
| `cp -r` | `cp -r папка папка/` | Скопировать папку рекурсивно | Стандартная терминала |
| `mv` | `mv старое_имя новое_имя` | Переименовать файл/папку | Стандарная терминала |
| `mv` | `mv имя новая_директория` | Переместить файл/папку | Стандартная терминала |
| `rm` | `rm имя_файла` | Удалить файл (Remove) | Стандартная терминала |
| `rm -r` | `rm -r имя_папки` | Удалить директорию с содержимым | Стандартная терминала |
| `rmdir` | `rmdir имя_папки` | Удалить пустую директорию | Стандартная терминала |

### Просмотр содержимого
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `cat` | `cat файл` | Показать всё содержимое файла | Стандартная терминала |
| `less` | `less файл` | Просматривать файл постранично | Стандартная терминала |
| `head` | `head файл` | Показать первые 10 строк файла | Стандартная терминала |
| `tail` | `tail файл` | Показать последние 10 строк файла | Стандартная терминала |
| `tail -f` | `tail -f файл` | Показывать новые строки в реальном времени | Стандартная терминала |

---

## ⚙️ Основные команды Git

### Начало работы
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git init` | `git init` | Инициализировать новый локальный репозиторий | Команда Git |
| `git clone` | `git clone URL [папка]` | Клонировать удалённый репозиторий | Команда Git |
| `git status` | `git status` | Показать состояние рабочей области и индекса | Команда Git |
| `git status -s` | `git status -s` | Сокращённый вывод статуса | Команда Git |

### Работа с изменениями
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git add` | `git add файл` | Добавить файл в индекс (подготовить к коммиту) | Команда Git |
| `git add .` | `git add .` | Добавить все изменённые файлы в текущей директории | Команда Git |
| `git commit` | `git commit` | Создать коммит (откроется редактор для сообщения) | Команда Git |
| `git commit -m` | `git commit -m "сообщение"` | Создать коммит с описанием | Команда Git |
| `git commit -am` | `git commit -am "сообщение"` | Добавить все отслеживаемые файлы и создать коммит | Команда Git |

---

## 📜 Просмотр истории

| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git log` | `git log` | Показать историю коммитов | Команда Git |
| `git log --oneline` | `git log --oneline` | Краткая история (хеш + сообщение) | Команда Git |
| `git log -p` | `git log -p` | История с показами изменений в коде | Команда Git |
| `git log --graph` | `git log --graph` | История с визуализацией ветвления | Команда Git |
| `git log --stat` | `git log --stat` | История со статистикой изменённых файлов | Команда Git |
| `git show` | `git show хеш` | Показать изменения конкретного коммита | Команда Git |
| `git blame` | `git blame файл` | Показать автора каждой строки файла | Команда Git |

---

## 🔀 Работа с ветками

| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git branch` | `git branch` | Показать локальные ветки | Команда Git |
| `git branch -a` | `git branch -a` | Показать все ветки (локальные и удалённые) | Команда Git |
| `git branch -v` | `git branch -v` | Показать ветки с последним коммитом | Команда Git |
| `git branch` | `git branch новая_ветка` | Создать новую ветку | Команда Git |
| `git switch` | `git switch ветка` | Переключиться на указанную ветку | Команда Git |
| `git switch -c` | `git switch -c новая_ветка` | Создать и переключиться на новую ветку | Команда Git |
| `git merge` | `git merge ветка` | Влить изменения из указанной ветки в текущую | Команда Git |
| `git branch -d` | `git branch -d ветка` | Удалить локальную ветку (только если изменения влиты) | Команда Git |
| `git branch -D` | `git branch -D ветка` | Принудительно удалить локальную ветку | Команда Git |

---

## ↩️ Откат изменений

### Область действия: Рабочая директория
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git restore` | `git restore файл` | Откатить изменения в файле до последнего коммита | Команда Git |
| `git restore .` | `git restore .` | Откатить все изменения в текущей директории | Команда Git |

### Область действия: Индекс (Staging Area)
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git restore --staged` | `git restore --staged файл` | Убрать файл из индекса (перевести в modified) | Команда Git |
| `git restore --staged .` | `git restore --staged .` | Убрать все файлы из индекса | Команда Git |
| `git reset` | `git reset` | Сбросить весь индекс (сохраняя изменения) | Команда Git |
| `git reset файл` | `git reset файл` | Убрать конкретный файл из индекса | Команда Git |

### Область действия: История коммитов
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git commit --amend` | `git commit --amend` | Добавить изменения к последнему коммиту | Команда Git |
| `git commit --amend --no-edit` | `git commit --amend --no-edit` | Добавить изменения без изменения сообщения | Команда Git |
| `git commit --amend -m` | `git commit --amend -m "новое"` | Изменить сообщение последнего коммита | Команда Git |

### Откат коммитов (reset)
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git reset --soft HEAD~1` | `git reset --soft HEAD~1` | Откат коммита с сохранением в индексе | Команда Git |
| `git reset --mixed HEAD~1` | `git reset --mixed HEAD~1` | Откат коммита с сохранением в рабочей директории | Команда Git |
| `git reset --hard HEAD~1` | `git reset --hard HEAD~1` | Полный откат коммита (удаляет изменения) | Команда Git |
| `git reset HEAD~2` | `git reset HEAD~2` | Отменить последние 2 коммита | Команда Git |

### Безопасный откат (revert)
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git revert` | `git revert хеш_коммита` | Создать коммит, отменяющий изменения | Команда Git |
| `git revert` | `git revert старший_хеш..младший_хеш` | Отменить диапазон коммитов | Команда Git |

### Восстановление после ошибок
| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git reflog` | `git reflog` | Показать историю всех перемещений HEAD | Команда Git |
| `git reset --hard ORIG_HEAD` | `git reset --hard ORIG_HEAD` | Вернуться к состоянию до опасной операции | Команда Git |
| `git reset --hard <commit>` | `git reset --hard <commit_id>` | Вернуться к конкретному коммиту из reflog | Команда Git |

---

## 🔍 Сравнение изменений

| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git diff` | `git diff` | Показать изменения в рабочей директории | Команда Git |
| `git diff --staged` | `git diff --staged` | Показать изменения в проиндексированных файлах | Команда Git |
| `git diff коммит1 коммит2` | `git diff хеш1 хеш2` | Показать разницу между двумя коммитами | Команда Git |
| `git diff ветка1 ветка2` | `git diff ветка1 ветка2` | Показать разницу между двумя ветками | Команда Git |
| `git diff HEAD` | `git diff HEAD` | Показать все изменения относительно последнего коммита | Команда Git |

---

## 🌐 Работа с удалёнными репозиториями

| Команда | Синтаксис | Описание | Тип команды |
|---------|-----------|----------|-------------|
| `git remote` | `git remote` | Показать список удалённых репозиториев | Команда Git |
| `git remote -v` | `git remote -v` | Показать удалённые репозитории с URL | Команда Git |
| `git remote add` | `git remote add имя URL` | Добавить удалённый репозиторий | Команда Git |
| `git remote remove` | `git remote remove имя` | Удалить удалённый репозиторий | Команда Git |
| `git fetch` | `git fetch имя_удалённого` | Получить информацию об изменениях (без слияния) | Команда Git |
| `git pull` | `git pull имя_удалённого ветка` | Получить изменения и автоматически слить | Команда Git |
| `git pull --rebase` | `git pull --rebase` | Получить изменения и переместить local коммиты поверх новых | Команда Git |
| `git push` | `git push имя_удалённого ветка` | Отправить изменения в удалённый репозиторий | Команда Git |
| `git push -u` | `git push -u origin ветка` | Отправить и установить upstream связь | Команда Git |
| `git push -d` | `git push origin -d ветка` | Удалить ветку в удалённом репозитории | Команда Git |

---

## 🧹 Очистка рабочей директории (git clean)

| Команда | Синтаксис | Описание | Тип команды | Когда использовать |
|---------|-----------|----------|-------------|-------------------|
| `git clean` | `git clean` | Интерактивная очистка (покажет что будет удалено) | Команда Git | Предварительный просмотр перед удалением |
| `git clean -n` | `git clean -n` | Пробный запуск (покажет что будет удалено, без реального удаления) | Команда Git | Безопасная проверка что будет очищено |
| `git clean -f` | `git clean -f` | Принудительная очистка удаляет неотслеживаемые файлы | Команда Git | Удалить все неотслеживаемые файлы |
| `git clean -f -d` | `git clean -f -d` | Очистка с директориями удаляет файлы и пустые папки | Команда Git | Когда нужно удалить и неотслеживаемые папки |
| `git clean -f -x` | `git clean -f -x` | Очистка включая игнорируемые файлы (из .gitignore) | Команда Git | Полная очистка, включая временные файлы |
| `git clean -f -X` | `git clean -f -X` | Только игнорируемые файлы удаляет только то, что в .gitignore | Команда Git | Очистка только кэша, логов и т.д. |
| `git clean -f -d -x` | `git clean -f -d -x` | Полная очистка файлов, папок и игнорируемых файлов | Команда Git | Полный сброс рабочей директории |

### 🔍 Разбор ключей:
- **`-n`** (--dry-run) - демо-режим, только показать что будет удалено
- **`-f`** (--force) - принудительное выполнение (обязательный флаг)
- **`-d`** - удалять неотслеживаемые директории вместе с содержимым
- **`-x`** - удалять файлы, включая те, что указаны в .gitignore
- **`-X`** - удалять ТОЛЬКО файлы, указанные в .gitignore