![Git](https://img.shields.io/badge/Git-F05032?style=for-the-badge&logo=git&logoColor=white)
![GitHub](https://img.shields.io/badge/GitHub-181717?style=for-the-badge&logo=github&logoColor=white)
![Terminal](https://img.shields.io/badge/Terminal-4D4D4D?style=for-the-badge&logo=windows-terminal&logoColor=white)
# 📚 Репозиторий для изучения Git

Репозиторий посвящен изучению системы контроля версий Git. Здесь собраны примеры, конспекты и обучающие материалы из различных источников:

- Открытый курс Яндекса по Git
- Официальная документация Git
- Другие открытые образовательные ресурсы

---

## 🐚 Основные команды Bash/Terminal

### Навигация и просмотр
| Команда       | Синтаксис                          | Описание                                                                 |
|---------------|------------------------------------|--------------------------------------------------------------------------|
| `pwd`         | `pwd`                              | Показать путь к текущей папке (Print Working Directory)                  |
| `cd`          | `cd путь/к/папке`                 | Сменить директорию (Change Directory)                                    |
| `cd ~`        | `cd ~`                             | Перейти в домашнюю директорию пользователя                               |
| `cd ..`       | `cd ..`                            | Подняться на уровень выше в директориях                                  |
| `ls`          | `ls`                               | Показать содержимое текущей директории (List)                            |
| `ls -a`       | `ls -a`                            | Показать все файлы, включая скрытые (все)                                |
| `ls -la`      | `ls -la`                           | Показать подробный список всех файлов (длинный формат, все файлы)        |

### Работа с файлами и директориями
| Команда       | Синтаксис                          | Описание                                                                 |
|---------------|------------------------------------|--------------------------------------------------------------------------|
| `touch`       | `touch имя_файла`                  | Создать новый пустой файл                                                |
| `mkdir`       | `mkdir имя_папки`                  | Создать новую директорию (Make Directory)                                |
| `mkdir -p`    | `mkdir -p путь/к/папке`           | Создать иерархию директорий (создает все промежуточные папки)            |
| `cp`          | `cp файл папка/`                   | Скопировать файл (Copy)                                                  |
| `cp -r`       | `cp -r папка папка/`               | Скопировать папку рекурсивно (вместе с содержимым)                       |
| `mv`          | `mv старое_имя новое_имя`         | Переименовать  файл/папку (rename)                          |
| `mv`          | `mv имя новая_директория`         | Переместить файл/папку (Move)                          |
| `mv`          | `mv имя новая_директория новое_имя`         | Переместить и переместить файл/папку (Move)                          |
| `rm`          | `rm имя_файла`                     | Удалить файл (Remove)                                                    |
| `rm -r`       | `rm -r имя_папки`                  | Удалить директорию с содержимым (рекурсивно)                             |
| `rmdir`       | `rmdir имя_папки`                  | Удалить пустую директорию                                                |

### Просмотр содержимого
| Команда       | Синтаксис                          | Описание                                                                 |
|---------------|------------------------------------|--------------------------------------------------------------------------|
| `cat`         | `cat файл`                         | Показать всё содержимое файла (Concatenate)                              |
| `less`        | `less файл`                        | Просматривать файл постранично (можно листать)                           |
| `head`        | `head файл`                        | Показать первые 10 строк файла                                           |
| `tail`        | `tail файл`                        | Показать последние 10 строк файла                                        |
| `tail -f`     | `tail -f файл`                     | Показывать новые строки в реальном времени (follow)                      |

---

## ⚙️ Основные команды Git

### Начало работы
| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git init`            | `git init`                         | Инициализировать новый локальный репозиторий                             |
| `git clone`           | `git clone URL [папка]`            | Клонировать удалённый репозиторий                                        |
| `git status`          | `git status`                       | Показать состояние рабочей области и индекса                             |
| `git status -s`       | `git status -s`                    | Сокращённый вывод статуса                                                |

### Работа с изменениями
| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git add`             | `git add файл`                     | Добавить файл в индекс (подготовить к коммиту)                           |
| `git add .`           | `git add .`                        | Добавить все изменённые файлы в текущей директории                       |
| `git commit`          | `git commit`                       | Создать коммит (откроется редактор для сообщения)                        |
| `git commit -m`       | `git commit -m "сообщение"`        | Создать коммит с описанием                                               |
| `git commit -am`      | `git commit -am "сообщение"`       | Добавить все отслеживаемые файлы и создать коммит (новые не добавятся)   |

---

## 📜 Просмотр истории

| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git log`             | `git log`                          | Показать историю коммитов                                                |
| `git log --oneline`   | `git log --oneline`                | Краткая история (хеш + сообщение)                                        |
| `git log -p`          | `git log -p`                       | История с показами изменений в коде (patch)                              |
| `git log --graph`     | `git log --graph`                  | История с визуализацией ветвления                                        |
| `git log --stat`      | `git log --stat`                   | История со статистикой изменённых файлов                                 |
| `git show`            | `git show хеш`                     | Показать изменения конкретного коммита                                   |
| `git blame`           | `git blame файл`                   | Показать автора каждой строки файла                                      |

---

## 🔀 Работа с ветками

| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git branch`          | `git branch`                       | Показать локальные ветки                                                 |
| `git branch -a`       | `git branch -a`                    | Показать все ветки (локальные и удалённые)                               |
| `git branch -v`       | `git branch -v`                    | Показать ветки с последним коммитом                                      |
| `git branch`          | `git branch новая_ветка`           | Создать новую ветку                                                      |
| `git switch`          | `git switch ветка`                 | Переключиться на указанную ветку                                         |
| `git switch -c`       | `git switch -c новая_ветка`        | Создать и переключиться на новую ветку                                   |
| `git merge`           | `git merge ветка`                  | Влить изменения из указанной ветки в текущую                             |
| `git branch -d`       | `git branch -d ветка`              | Удалить локальную ветку (только если изменения влиты)                    |
| `git branch -D`       | `git branch -D ветка`              | Принудительно удалить локальную ветку                                    |

---

## ↩️ Откат изменений

### Область действия: Рабочая директория
| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git restore`         | `git restore файл`                 | Откатить изменения в файле до последнего коммита                         |
| `git restore .`       | `git restore .`                    | Откатить все изменения в текущей директории                              |

### Область действия: Индекс (Staging Area)
| Команда                     | Синтаксис                          | Описание                                                                 |
|-----------------------------|------------------------------------|--------------------------------------------------------------------------|
| `git restore --staged`      | `git restore --staged файл`        | Убрать файл из индекса (перевести в modified)                            |
| `git restore --staged .`    | `git restore --staged .`           | Убрать все файлы из индекса                                              |
| `git reset`                 | `git reset`                        | Сбросить весь индекс (сохраняя изменения в рабочей директории)           |
| `git reset файл`            | `git reset файл`                   | Убрать конкретный файл из индекса                                        |

### Область действия: История коммитов
| Команда                     | Синтаксис                          | Описание                                                                 |
|-----------------------------|------------------------------------|--------------------------------------------------------------------------|
| `git commit --amend`        | `git commit --amend`               | Добавить изменения к последнему коммиту (откроется редактор)             |
| `git commit --amend --no-edit` | `git commit --amend --no-edit`  | Добавить изменения к последнему коммиту без изменения сообщения          |
| `git commit --amend -m`     | `git commit --amend -m "новое"`    | Изменить сообщение последнего коммита                                    |

### Откат коммитов (reset)
| Команда                     | Синтаксис                          | Описание                                                                 |
|-----------------------------|------------------------------------|--------------------------------------------------------------------------|
| `git reset --soft HEAD~1`   | `git reset --soft HEAD~1`          | Откат коммита с сохранением изменений в индексе                          |
| `git reset --mixed HEAD~1`  | `git reset --mixed HEAD~1`         | Откат коммита с сохранением изменений в рабочей директории (по умолч.)   |
| `git reset --hard HEAD~1`   | `git reset --hard HEAD~1`          | Полный откат коммита (удаляет изменения)                                 |
| `git reset HEAD~2`          | `git reset HEAD~2`                 | Отменить последние 2 коммита (с сохранением изменений)                   |
| `git reset --hard HEAD~2`   | `git reset --hard HEAD~2`          | Отменить последние 2 коммита (удаляя изменения)                          |

### Безопасный откат (revert)
| Команда                     | Синтаксис                          | Описание                                                                 |
|-----------------------------|------------------------------------|--------------------------------------------------------------------------|
| `git revert`                | `git revert хеш_коммита`           | Создать новый коммит, отменяющий изменения указанного коммита            |
| `git revert`                | `git revert старший_хеш..младший_хеш` | Отменить диапазон коммитов (создаст несколько коммитов)                |

### Восстановление после ошибок
| Команда                     | Синтаксис                          | Описание                                                                 |
|-----------------------------|------------------------------------|--------------------------------------------------------------------------|
| `git reflog`                | `git reflog`                       | Показать историю всех перемещений HEAD                                   |
| `git reset --hard ORIG_HEAD`| `git reset --hard ORIG_HEAD`       | Вернуться к состоянию до опасной операции                                |
| `git reset --hard <commit>` | `git reset --hard <commit_id>`     | Вернуться к конкретному коммиту из reflog                                |

---

## 🔍 Сравнение изменений

| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git diff`            | `git diff`                         | Показать изменения в рабочей директории (непроиндексированные)           |
| `git diff --staged`   | `git diff --staged`                | Показать изменения в проиндексированных файлах                           |
| `git diff коммит1 коммит2` | `git diff хеш1 хеш2`         | Показать разницу между двумя коммитами                                   |
| `git diff ветка1 ветка2` | `git diff ветка1 ветка2`       | Показать разницу между двумя ветками                                     |
| `git diff HEAD`       | `git diff HEAD`                    | Показать все изменения относительно последнего коммита                   |

---

## 🌐 Работа с удалёнными репозиториями

| Команда               | Синтаксис                          | Описание                                                                 |
|-----------------------|------------------------------------|--------------------------------------------------------------------------|
| `git remote`          | `git remote`                       | Показать список удалённых репозиториев                                   |
| `git remote -v`       | `git remote -v`                    | Показать удалённые репозитории с URL                                     |
| `git remote add`      | `git remote add имя URL`           | Добавить удалённый репозиторий                                           |
| `git remote remove`   | `git remote remove имя`            | Удалить удалённый репозиторий                                            |
| `git fetch`           | `git fetch имя_удалённого`         | Получить информацию об изменениях (без слияния)                          |
| `git pull`            | `git pull имя_удалённого ветка`    | Получить изменения и автоматически слить                                 |
| `git pull --rebase`   | `git pull --rebase`                | Получить изменения и переместить local коммиты поверх новых              |
| `git push`            | `git push имя_удалённого ветка`    | Отправить изменения в удалённый репозиторий                              |
| `git push -u`         | `git push -u origin ветка`         | Отправить и установить upstream связь                                    |
| `git push -d`         | `git push origin -d ветка`         | Удалить ветку в удалённом репозитории                                    |

## 🧹 Очистка рабочей директории (git clean)

| Команда | Синтаксис | Описание | Когда использовать |
|---------|-----------|----------|-------------------|
| **`git clean`** | `git clean` | **Интерактивная очистка** (покажет что будет удалено) | Предварительный просмотр перед удалением |
| **`git clean -n`** | `git clean -n` | **Пробный запуск** (покажет что будет удалено, без реального удаления) | Безопасная проверка что будет очищено |
| **`git clean -f`** | `git clean -f` | **Принудительная очистка** удаляет неотслеживаемые файлы | Удалить все неотслеживаемые файлы |
| **`git clean -f -d`** | `git clean -f -d` | **Очистка с директориями** удаляет файлы и пустые папки | Когда нужно удалить и неотслеживаемые папки |
| **`git clean -f -x`** | `git clean -f -x` | **Очистка включая игнорируемые файлы** (из .gitignore) | Полная очистка, включая временные файлы |
| **`git clean -f -X`** | `git clean -f -X` | **Только игнорируемые файлы** удаляет только то, что в .gitignore | Очистка только кэша, логов и т.д. |
| **`git clean -f -d -x`** | `git clean -f -d -x` | **Полная очистка** файлов, папок и игнорируемых файлов | Полный сброс рабочей директории |

### 🔍 Разбор ключей:
- **`-n`** (--dry-run) - демо-режим, только показать что будет удалено
- **`-f`** (--force) - принудительное выполнение (обязательный флаг)
- **`-d`** - удалять неотслеживаемые директории вместе с содержимым
- **`-x`** - удалять файлы, включая те, что указаны в .gitignore
- **`-X`** - удалять ТОЛЬКО файлы, указанные в .gitignore  


